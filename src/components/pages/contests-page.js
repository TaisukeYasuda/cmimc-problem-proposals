import React from "react";
import { Row, Col } from "react-materialize";
import {  } from "../utilities";

const ContestsPage = ({ message }) => (
  <Row className="container">
    <h1>Hi</h1>
  </Row>
);

export default ContestsPage;
